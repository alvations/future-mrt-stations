You are writing a single web search query to find the primary source that
supports a specific claim about Singapore's rail network.

Claim:
{{text}}

Write one search query. Use the words most likely to appear in the title of the
source document itself, such as an agency name, a project name, a station name
or a figure. Do not use search operators. Keep it under 15 words.

Reply with only this JSON object and nothing else:

{"query": "your search query"}
