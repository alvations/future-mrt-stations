You are computing one row of a demand index for a report on Singapore's rail
network.

The Demand Gap Index for an area is the sum over its components of:

    H x A x T x C

where, for each component:

- H is the number of homes needing rail service, in thousands
- A is the access gap, 0 to 1
- T is the timing gap, 0 to 1
- C is a crowding multiplier

Area: {{name}}

Components:
{{components}}

Compute the index. Sum every component. Round to one decimal place.

Reply with only this JSON object and nothing else:

{"dgi": 0.0}
